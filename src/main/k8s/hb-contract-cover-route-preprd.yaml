apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: contract-cover-route-preprod
spec:
  host: contract-cover.apps.osft4oa-rhaf.colsanitas.com
  to:
    kind: Service
    name: contract-cover-service
  port:
    targetPort: 8080